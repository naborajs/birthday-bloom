import { Phase } from "./CakeTypes";

/**
 * Note: CakeKnife is now rendered in true 3D WebGL space inside Cake3D via CakeKnife3D.
 * This component is maintained for backward compatibility.
 */
export const CakeKnife = (_props: { phase: Phase }) => {
    return null;
};
